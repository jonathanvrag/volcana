services:
  db:
    image: postgres:16
    container_name: volcana-db
    environment:
      POSTGRES_USER: volcana
      POSTGRES_PASSWORD: volcana
      POSTGRES_DB: volcana
    volumes:
      - db_data:/var/lib/postgresql/data
      - ../database/init-db.sql:/docker-entrypoint-initdb.d/init-db.sql:ro
    networks:
      - volcana-net
    restart: unless-stopped

  backend:
    build:
      context: ../backend
    container_name: volcana-backend
    env_file:
      - ../backend/.env
    depends_on:
      - db
    networks:
      - volcana-net
    volumes:
      - media_data:/app/media
    restart: unless-stopped

  frontend:
    build:
      context: ../frontend
    container_name: volcana-frontend
    depends_on:
      - backend
    networks:
      - volcana-net
    restart: unless-stopped

  nginx:
    image: nginx:alpine
    container_name: volcana-core-nginx
    depends_on:
      - frontend
      - backend
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - media_data:/usr/share/nginx/media:ro  # ? AGREGAR ESTA L�NEA
    ports:
      - '8080:80'
    networks:
      - volcana-net
    restart: unless-stopped

volumes:
  db_data:
  media_data:

networks:
  volcana-net:
    driver: bridge
